# Bot-only package

This archive contains only `services/bot` (+ docs) from the full TheCouncilAI stack.

If you want one-command install of the full stack (brain + control_api + deploy), use the full package zip.
